import { Sparkles } from 'lucide-react';

const WelcomeMessage = () => {
  return (
    <div className="text-center py-4 px-4 relative font-sora">
      {/* Empty component, keeping minimal structure for future updates */}
    </div>
  );
};

export default WelcomeMessage;
